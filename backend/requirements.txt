# Pose2Sim pulls its full scientific stack (opensim, rtmlib, openvino, pandas,
# scipy, etc.) transitively. Pinned to the versions validated on macOS arm64 /
# Python 3.12. opensim 4.6 ships a universal2 wheel — no conda required.
pose2sim==0.10.47
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.*
pytest==8.*
